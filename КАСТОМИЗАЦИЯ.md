# 🎨 Руководство по кастомизации

Это руководство поможет вам настроить лендинг под свои нужды.

---

## 📞 Изменение контактов

### Email и телефон

**Файл:** `app/page.tsx`

Найдите и замените в 2 местах:

1. **В секции CTA** (строка ~335):
```typescript
<motion.a
  href="mailto:info@lavia.agency"  // ← ИЗМЕНИТЕ
  ...
>
  Связаться с нами
</motion.a>
<motion.a
  href="tel:+79999999999"  // ← ИЗМЕНИТЕ
  ...
>
  +7 (999) 999-99-99  // ← ИЗМЕНИТЕ (отображаемый текст)
</motion.a>
```

2. **В Footer** (строка ~365):
```typescript
<p className="text-gray-400 mb-2">Email: info@lavia.agency</p>  // ← ИЗМЕНИТЕ
<p className="text-gray-400 mb-2">Телефон: +7 (999) 999-99-99</p>  // ← ИЗМЕНИТЕ
```

---

## 🎨 Изменение цветов

### Основная цветовая схема

**Файл:** `tailwind.config.ts`

```typescript
colors: {
  primary: {
    50: '#fef2f2',   // Самый светлый
    100: '#fee2e2',
    200: '#fecaca',
    300: '#fca5a5',
    400: '#f87171',
    500: '#ef4444',  // Базовый
    600: '#dc2626',  // Основной (текущий)
    700: '#b91c1c',
    800: '#991b1b',
    900: '#7f1d1d',  // Самый темный
  },
},
```

**Примеры других цветовых схем:**

### Синяя схема
```typescript
colors: {
  primary: {
    600: '#2563eb', // синий
    700: '#1d4ed8',
  },
},
```

### Зеленая схема
```typescript
colors: {
  primary: {
    600: '#16a34a', // зеленый
    700: '#15803d',
  },
},
```

### Фиолетовая схема
```typescript
colors: {
  primary: {
    600: '#9333ea', // фиолетовый
    700: '#7e22ce',
  },
},
```

После изменения цветов в `tailwind.config.ts`, найдите в `app/page.tsx` все:
- `text-red-` → замените на `text-primary-`
- `bg-red-` → замените на `bg-primary-`
- `from-red-` → замените на `from-primary-`

---

## ✏️ Изменение текстов

### Название компании

**Файл:** `app/page.tsx`

```typescript
// Hero Section (строка ~54)
<motion.h1 className="text-6xl md:text-8xl font-bold mb-6">
  ЛАВИЯ  // ← ИЗМЕНИТЕ
</motion.h1>
```

**Файл:** `app/layout.tsx`

```typescript
export const metadata: Metadata = {
  title: "Лавия — Рекламное агентство полного цикла",  // ← ИЗМЕНИТЕ
  description: "...",  // ← ИЗМЕНИТЕ
};
```

### Слоган

**Файл:** `app/page.tsx` (строка ~63)

```typescript
<motion.p className="text-xl md:text-3xl mb-8 font-light">
  Рекламное агентство полного цикла  // ← ИЗМЕНИТЕ
</motion.p>
```

### Описание

**Файл:** `app/page.tsx` (строка ~70)

```typescript
<motion.p className="text-lg md:text-xl mb-12 max-w-3xl mx-auto leading-relaxed">
  Мы помогаем брендам обрести голос...  // ← ИЗМЕНИТЕ
</motion.p>
```

---

## 🎯 Изменение услуг

**Файл:** `app/page.tsx` (строка ~115)

```typescript
{[
  {
    icon: <TrendingUp className="w-12 h-12" />,
    title: "Цифровой маркетинг",  // ← ИЗМЕНИТЕ
    description: "Комплексное продвижение..."  // ← ИЗМЕНИТЕ
  },
  // Добавьте или удалите услуги по необходимости
].map((service, index) => (...))}
```

**Доступные иконки** (из lucide-react):
- `TrendingUp`, `Target`, `Lightbulb`, `Zap`
- `Award`, `Heart`, `Users`, `Briefcase`
- `Camera`, `Monitor`, `Smartphone`, `Globe`
- `Mail`, `MessageCircle`, `Search`, `Edit`

Полный список: [lucide.dev/icons](https://lucide.dev/icons)

---

## 💎 Изменение ценностей

**Файл:** `app/page.tsx` (строка ~165)

```typescript
{[
  {
    icon: <Award className="w-12 h-12" />,
    title: "Результат",  // ← ИЗМЕНИТЕ
    description: "Мы нацелены на измеримый эффект..."  // ← ИЗМЕНИТЕ
  },
  // ... остальные ценности
]}
```

---

## ⭐ Изменение преимуществ

**Файл:** `app/page.tsx` (строка ~245)

```typescript
{[
  {
    icon: <Briefcase className="w-10 h-10" />,
    title: "Интересные проекты",  // ← ИЗМЕНИТЕ
    description: "Вы будете работать..."  // ← ИЗМЕНИТЕ
  },
  // ... остальные преимущества
]}
```

---

## 🖼️ Добавление логотипа/favicon

### Favicon

1. Создайте иконку 32x32px или 512x512px
2. Сохраните как `favicon.ico`
3. Замените файл `public/favicon.ico`

### Логотип в Header

Добавьте логотип в Hero Section:

```typescript
<motion.div className="flex justify-center mb-8">
  <img 
    src="/logo.png" 
    alt="Лавия" 
    className="h-24 w-auto"
  />
</motion.div>
<motion.h1 className="text-6xl md:text-8xl font-bold mb-6">
  ЛАВИЯ
</motion.h1>
```

---

## 🌐 Изменение социальных сетей

**Файл:** `app/page.tsx` (строка ~375)

```typescript
<div className="flex gap-4">
  <a href="https://vk.com/вашастраница" className="...">VK</a>
  <a href="https://t.me/вашканал" className="...">Telegram</a>
  <a href="https://instagram.com/вашпрофиль" className="...">Instagram*</a>
  
  {/* Добавьте новые: */}
  <a href="https://facebook.com/..." className="...">Facebook</a>
  <a href="https://youtube.com/..." className="...">YouTube</a>
</div>
```

---

## 🎬 Настройка анимаций

### Скорость анимаций

**Файл:** `app/page.tsx`

Найдите `transition={{ duration: 0.6 }}` и измените значение:
- `0.3` - быстро
- `0.6` - средне (по умолчанию)
- `1.0` - медленно

### Отключение анимаций

Удалите или закомментируйте:
```typescript
// initial={{ opacity: 0, y: 30 }}
// whileInView={{ opacity: 1, y: 0 }}
// viewport={{ once: true }}
// transition={{ duration: 0.6 }}
```

---

## 📐 Изменение отступов

**Файл:** `app/page.tsx`

Все секции имеют класс `py-24` (padding vertical 24 = 96px)

Измените на:
- `py-12` - маленькие отступы (48px)
- `py-16` - средние отступы (64px)
- `py-24` - большие отступы (96px) [текущие]
- `py-32` - очень большие (128px)

---

## 🔤 Изменение шрифта

**Файл:** `app/layout.tsx`

```typescript
import { Inter } from "next/font/google";

// Замените на другой шрифт:
import { Roboto } from "next/font/google";
import { Montserrat } from "next/font/google";
import { Poppins } from "next/font/google";

const font = Roboto({ 
  weight: ['300', '400', '700'],
  subsets: ["latin", "cyrillic"] 
});
```

Список шрифтов: [fonts.google.com](https://fonts.google.com)

---

## 🎨 Изменение градиентов

### Hero Section

**Файл:** `app/page.tsx` (строка ~27)

```typescript
className="... bg-gradient-to-br from-red-600 via-red-500 to-pink-500 ..."

// Варианты:
// Синий: from-blue-600 via-blue-500 to-cyan-500
// Зеленый: from-green-600 via-green-500 to-emerald-500
// Фиолетовый: from-purple-600 via-purple-500 to-pink-500
// Оранжевый: from-orange-600 via-red-500 to-pink-500
```

---

## 📱 Настройка адаптивности

### Брейкпоинты

Tailwind использует:
- `sm:` - 640px+
- `md:` - 768px+
- `lg:` - 1024px+
- `xl:` - 1280px+
- `2xl:` - 1536px+

**Пример:**
```typescript
className="text-2xl md:text-4xl lg:text-6xl"
// mobile: 2xl
// tablet: 4xl
// desktop: 6xl
```

---

## 📊 Добавление Google Analytics

**Файл:** `app/layout.tsx`

Добавьте перед закрывающим `</body>`:

```typescript
import Script from 'next/script'

// В компонент:
<Script
  src="https://www.googletagmanager.com/gtag/js?id=GA_MEASUREMENT_ID"
  strategy="afterInteractive"
/>
<Script id="google-analytics" strategy="afterInteractive">
  {`
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());
    gtag('config', 'GA_MEASUREMENT_ID');
  `}
</Script>
```

---

## 🎯 Изменение кнопок

### Стиль кнопок

**Файл:** `app/page.tsx`

Найдите классы кнопок:
```typescript
className="bg-white text-red-600 px-10 py-4 rounded-full ..."

// Варианты rounded:
// rounded-none - без скругления
// rounded - маленькое
// rounded-lg - среднее
// rounded-full - полное (текущее)

// Варианты размера:
// px-6 py-2 - маленькая
// px-8 py-3 - средняя
// px-10 py-4 - большая (текущая)
```

---

## 🔧 Быстрые изменения через поиск

### Замена всего текста "Лавия"

1. Откройте `app/page.tsx`
2. Ctrl + H (найти и заменить)
3. Найти: `Лавия`
4. Заменить на: `Ваше название`
5. Заменить все

### Замена email во всем проекте

1. Ctrl + Shift + F (поиск по всем файлам)
2. Найти: `info@lavia.agency`
3. Заменить на: `ваш@email.com`
4. Заменить все

---

## ✅ Чеклист кастомизации

- [ ] Название компании изменено
- [ ] Контакты обновлены (email, телефон)
- [ ] Услуги актуализированы
- [ ] Ценности обновлены
- [ ] Преимущества изменены
- [ ] Цвета настроены (опционально)
- [ ] Favicon заменен
- [ ] Социальные сети добавлены
- [ ] Форма настроена
- [ ] Аналитика добавлена (опционально)

---

## 💡 Советы

1. **Меняйте постепенно** - сначала тексты, потом цвета
2. **Сохраняйте оригинал** - сделайте копию файла перед большими изменениями
3. **Тестируйте сразу** - запускайте `npm run dev` и смотрите результат
4. **Используйте DevTools** - F12 для проверки мобильной версии
5. **Коммитьте изменения** - `git commit` после каждого этапа

---

**Успехов в кастомизации! 🎨**


