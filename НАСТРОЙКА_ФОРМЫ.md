# 📧 Настройка формы обратной связи

Форма уже встроена в лендинг, но нужно настроить отправку данных.

---

## Вариант 1: Email через EmailJS (Рекомендуется) ⭐

**Преимущества:**
- ✅ Бесплатно до 200 писем/месяц
- ✅ Не требует backend
- ✅ Настройка за 5 минут

### Шаг 1: Регистрация
1. Перейдите на [emailjs.com](https://www.emailjs.com)
2. Создайте аккаунт
3. Подтвердите email

### Шаг 2: Настройка сервиса
1. В панели нажмите "Email Services"
2. Нажмите "Add New Service"
3. Выберите ваш email провайдер (Gmail, Outlook и т.д.)
4. Подключите аккаунт
5. Скопируйте **Service ID**

### Шаг 3: Создание шаблона
1. Перейдите в "Email Templates"
2. Нажмите "Create New Template"
3. Используйте эти переменные:
```
Имя: {{name}}
Email: {{email}}
Телефон: {{phone}}
Сообщение: {{message}}
```
4. Скопируйте **Template ID**

### Шаг 4: Получите Public Key
1. Перейдите в "Account" → "General"
2. Скопируйте **Public Key**

### Шаг 5: Установите библиотеку
```bash
npm install @emailjs/browser
```

### Шаг 6: Обновите ContactForm.tsx

Замените функцию `handleSubmit`:

```typescript
import emailjs from '@emailjs/browser';

const handleSubmit = async (e: React.FormEvent) => {
  e.preventDefault();
  setIsSubmitting(true);

  try {
    await emailjs.send(
      'YOUR_SERVICE_ID',     // Замените
      'YOUR_TEMPLATE_ID',    // Замените
      {
        name: formData.name,
        email: formData.email,
        phone: formData.phone,
        message: formData.message,
      },
      'YOUR_PUBLIC_KEY'      // Замените
    );

    setSubmitStatus("success");
    setFormData({ name: "", email: "", phone: "", message: "" });
  } catch (error) {
    console.error("Error:", error);
    setSubmitStatus("error");
  } finally {
    setIsSubmitting(false);
    setTimeout(() => setSubmitStatus("idle"), 3000);
  }
};
```

---

## Вариант 2: Telegram Bot

**Преимущества:**
- ✅ Бесплатно и без ограничений
- ✅ Мгновенные уведомления
- ✅ Простая настройка

### Шаг 1: Создайте бота
1. Найдите в Telegram: @BotFather
2. Отправьте команду: `/newbot`
3. Следуйте инструкциям
4. Получите **TOKEN**

### Шаг 2: Получите Chat ID
1. Найдите бота: @userinfobot
2. Нажмите Start
3. Скопируйте ваш **Chat ID**

### Шаг 3: Создайте API Route

Создайте файл `app/api/contact/route.ts`:

```typescript
import { NextResponse } from 'next/server';

export async function POST(request: Request) {
  try {
    const { name, email, phone, message } = await request.json();

    const TELEGRAM_BOT_TOKEN = 'YOUR_BOT_TOKEN';
    const TELEGRAM_CHAT_ID = 'YOUR_CHAT_ID';

    const text = `
🔔 Новая заявка с сайта!

👤 Имя: ${name}
📧 Email: ${email}
📱 Телефон: ${phone}
💬 Сообщение: ${message}
    `;

    const telegramUrl = `https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage`;

    await fetch(telegramUrl, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({
        chat_id: TELEGRAM_CHAT_ID,
        text: text,
        parse_mode: 'HTML',
      }),
    });

    return NextResponse.json({ success: true });
  } catch (error) {
    console.error('Error:', error);
    return NextResponse.json(
      { success: false, error: 'Failed to send message' },
      { status: 500 }
    );
  }
}
```

### Шаг 4: Обновите ContactForm.tsx

```typescript
const handleSubmit = async (e: React.FormEvent) => {
  e.preventDefault();
  setIsSubmitting(true);

  try {
    const response = await fetch('/api/contact', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
      },
      body: JSON.stringify(formData),
    });

    if (response.ok) {
      setSubmitStatus("success");
      setFormData({ name: "", email: "", phone: "", message: "" });
    } else {
      setSubmitStatus("error");
    }
  } catch (error) {
    console.error("Error:", error);
    setSubmitStatus("error");
  } finally {
    setIsSubmitting(false);
    setTimeout(() => setSubmitStatus("idle"), 3000);
  }
};
```

---

## Вариант 3: Google Sheets

**Преимущества:**
- ✅ Данные сохраняются в таблице
- ✅ Легко экспортировать
- ✅ Бесплатно

### Шаг 1: Создайте Google Sheet
1. Создайте новую таблицу
2. Добавьте заголовки: Дата, Имя, Email, Телефон, Сообщение

### Шаг 2: Apps Script
1. Extensions → Apps Script
2. Вставьте код:

```javascript
function doPost(e) {
  var sheet = SpreadsheetApp.getActiveSheet();
  var data = JSON.parse(e.postData.contents);
  
  sheet.appendRow([
    new Date(),
    data.name,
    data.email,
    data.phone,
    data.message
  ]);
  
  return ContentService.createTextOutput(
    JSON.stringify({ success: true })
  ).setMimeType(ContentService.MimeType.JSON);
}
```

3. Deploy → New Deployment
4. Type: Web App
5. Execute as: Me
6. Who has access: Anyone
7. Скопируйте URL

### Шаг 3: Обновите форму

```typescript
const handleSubmit = async (e: React.FormEvent) => {
  e.preventDefault();
  setIsSubmitting(true);

  try {
    const response = await fetch('YOUR_GOOGLE_SCRIPT_URL', {
      method: 'POST',
      body: JSON.stringify(formData),
    });

    if (response.ok) {
      setSubmitStatus("success");
      setFormData({ name: "", email: "", phone: "", message: "" });
    }
  } catch (error) {
    setSubmitStatus("error");
  } finally {
    setIsSubmitting(false);
    setTimeout(() => setSubmitStatus("idle"), 3000);
  }
};
```

---

## Вариант 4: Formspree

**Преимущества:**
- ✅ Простейшая настройка
- ✅ Бесплатно до 50 заявок/месяц
- ✅ Не требует кода

### Шаги:
1. Зайдите на [formspree.io](https://formspree.io)
2. Зарегистрируйтесь
3. Создайте новую форму
4. Получите endpoint URL

### Обновите форму:

```typescript
const handleSubmit = async (e: React.FormEvent) => {
  e.preventDefault();
  setIsSubmitting(true);

  try {
    const response = await fetch('https://formspree.io/f/YOUR_FORM_ID', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
      },
      body: JSON.stringify(formData),
    });

    if (response.ok) {
      setSubmitStatus("success");
      setFormData({ name: "", email: "", phone: "", message: "" });
    }
  } catch (error) {
    setSubmitStatus("error");
  } finally {
    setIsSubmitting(false);
    setTimeout(() => setSubmitStatus("idle"), 3000);
  }
};
```

---

## Вариант 5: Собственный Backend

Если у вас есть backend:

### API Route (`app/api/contact/route.ts`):

```typescript
import { NextResponse } from 'next/server';
import nodemailer from 'nodemailer';

export async function POST(request: Request) {
  const { name, email, phone, message } = await request.json();

  // Настройте транспортер
  const transporter = nodemailer.createTransport({
    host: 'smtp.gmail.com',
    port: 587,
    secure: false,
    auth: {
      user: process.env.EMAIL_USER,
      pass: process.env.EMAIL_PASSWORD,
    },
  });

  try {
    await transporter.sendMail({
      from: process.env.EMAIL_USER,
      to: 'info@lavia.agency',
      subject: 'Новая заявка с сайта',
      html: `
        <h2>Новая заявка</h2>
        <p><strong>Имя:</strong> ${name}</p>
        <p><strong>Email:</strong> ${email}</p>
        <p><strong>Телефон:</strong> ${phone}</p>
        <p><strong>Сообщение:</strong> ${message}</p>
      `,
    });

    return NextResponse.json({ success: true });
  } catch (error) {
    return NextResponse.json(
      { success: false, error: 'Failed to send email' },
      { status: 500 }
    );
  }
}
```

### Установите зависимости:
```bash
npm install nodemailer
npm install --save-dev @types/nodemailer
```

### Создайте `.env.local`:
```
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
```

---

## 🎯 Рекомендация

**Для быстрого старта:** Используйте **EmailJS** (Вариант 1)

**Для максимальной гибкости:** Используйте **Telegram Bot** (Вариант 2)

**Для простоты:** Используйте **Formspree** (Вариант 4)

---

## ✅ Чеклист настройки

- [ ] Выбран сервис для отправки
- [ ] Установлены необходимые пакеты
- [ ] Обновлен ContactForm.tsx
- [ ] Протестирована отправка формы
- [ ] Email приходит корректно
- [ ] Форма показывает успех/ошибку

---

**Готово! Теперь ваша форма работает! 🎉**


