# 🎨 Дополнительные компоненты

В проекте созданы дополнительные компоненты, которые можно добавить на лендинг.

---

## 📊 Stats (Статистика)

**Файл:** `app/components/Stats.tsx`

**Описание:** Секция с анимированными цифрами, которые "накручиваются" при просмотре.

**Возможности:**
- ✨ Анимированный счетчик
- 📊 4 показателя с цифрами
- 🎯 Настраиваемые значения
- 💫 Срабатывает при прокрутке

**Как добавить:**

1. Импортируйте компонент в `app/page.tsx`:
```typescript
import Stats from "./components/Stats";
```

2. Добавьте в нужное место (например, после секции Services):
```typescript
<Stats />
```

**Настройка:**

В файле `app/components/Stats.tsx` измените значения:
```typescript
<StatItem end={500} label="Довольных клиентов" suffix="+" />
<StatItem end={1200} label="Успешных проектов" suffix="+" />
<StatItem end={15} label="Лет на рынке" suffix="+" />
<StatItem end={50} label="Специалистов в команде" suffix="+" />
```

Параметры:
- `end` - конечное число
- `label` - подпись под числом
- `suffix` - суффикс (например, "+", "%", "K")
- `duration` - длительность анимации в секундах (по умолчанию 2)

---

## ⬆️ ScrollToTop (Кнопка "Наверх")

**Файл:** `app/components/ScrollToTop.tsx`

**Описание:** Плавающая кнопка для быстрой прокрутки вверх страницы.

**Возможности:**
- ⬆️ Плавная прокрутка к началу
- 👁️ Появляется после прокрутки вниз (300px)
- ✨ Анимация появления/исчезновения
- 🎨 Hover эффекты

**Как добавить:**

1. Импортируйте компонент в `app/page.tsx`:
```typescript
import ScrollToTop from "./components/ScrollToTop";
```

2. Добавьте в самый конец компонента (перед закрывающим тегом `</main>`):
```typescript
export default function Home() {
  return (
    <main className="min-h-screen">
      {/* ... все остальные секции ... */}
      
      <ScrollToTop />
    </main>
  );
}
```

**Настройка:**

Измените порог появления кнопки (по умолчанию 300px):
```typescript
if (window.pageYOffset > 500) { // появится после 500px
  setIsVisible(true);
}
```

Измените цвет кнопки:
```typescript
className="... bg-red-600 hover:bg-red-700 ..." // замените на нужный цвет
```

---

## 🎯 Как добавить оба компонента сразу

### Шаг 1: Обновите imports в `app/page.tsx`

Найдите секцию с импортами в начале файла и добавьте:

```typescript
import ContactForm from "./components/ContactForm";
import ScrollToTop from "./components/ScrollToTop";
import Stats from "./components/Stats";
```

### Шаг 2: Добавьте Stats после Services

Найдите секцию Services и добавьте Stats после неё:

```typescript
{/* Services Section */}
<section className="py-24 bg-white">
  {/* ... код секции Services ... */}
</section>

{/* Stats Section */}
<Stats />

{/* Values Section */}
<section className="py-24 bg-gradient-to-br from-gray-900 via-gray-800 to-black text-white">
  {/* ... код секции Values ... */}
</section>
```

### Шаг 3: Добавьте ScrollToTop в конец

В самом конце компонента, перед закрывающим `</main>`:

```typescript
      {/* Footer */}
      <footer className="bg-gray-900 text-white py-12">
        {/* ... код footer ... */}
      </footer>
      
      {/* Scroll To Top Button */}
      <ScrollToTop />
    </main>
  );
}
```

---

## 📝 Полный пример интеграции

```typescript
"use client";

import { motion } from "framer-motion";
import { /* ...все иконки... */ } from "lucide-react";
import ContactForm from "./components/ContactForm";
import ScrollToTop from "./components/ScrollToTop";
import Stats from "./components/Stats";

export default function Home() {
  return (
    <main className="min-h-screen">
      {/* Hero Section */}
      <section>...</section>

      {/* Services Section */}
      <section>...</section>

      {/* Stats Section - НОВОЕ! */}
      <Stats />

      {/* Values Section */}
      <section>...</section>

      {/* Why Join Section */}
      <section>...</section>

      {/* Contact Form Section */}
      <section>...</section>

      {/* CTA Section */}
      <section>...</section>

      {/* Footer */}
      <footer>...</footer>

      {/* Scroll To Top - НОВОЕ! */}
      <ScrollToTop />
    </main>
  );
}
```

---

## 🎨 Идеи для других компонентов

Вот что еще можно добавить:

### 1. Testimonials (Отзывы клиентов)
```typescript
// app/components/Testimonials.tsx
- Карточки с отзывами
- Фото клиентов
- Рейтинг звездами
- Слайдер отзывов
```

### 2. Portfolio/Cases (Портфолио)
```typescript
// app/components/Portfolio.tsx
- Галерея работ
- Фильтр по категориям
- Модальные окна с деталями
- Hover эффекты
```

### 3. Team (Команда)
```typescript
// app/components/Team.tsx
- Карточки сотрудников
- Фото и должности
- Соц. сети
- Hover эффекты
```

### 4. FAQ (Частые вопросы)
```typescript
// app/components/FAQ.tsx
- Аккордеон с вопросами
- Анимация раскрытия
- Иконки +/-
- Категории вопросов
```

### 5. Pricing (Цены)
```typescript
// app/components/Pricing.tsx
- Карточки тарифов
- Список возможностей
- Кнопки "Выбрать"
- Популярный тариф
```

### 6. Blog Preview (Превью блога)
```typescript
// app/components/BlogPreview.tsx
- 3 последние статьи
- Изображения
- Дата и автор
- Кнопка "Читать далее"
```

### 7. Partners (Партнеры)
```typescript
// app/components/Partners.tsx
- Логотипы компаний
- Бегущая строка
- Hover эффекты
- Ссылки на сайты
```

### 8. Newsletter (Подписка)
```typescript
// app/components/Newsletter.tsx
- Форма подписки на рассылку
- Email поле
- Кнопка "Подписаться"
- Уведомление об успехе
```

---

## 📥 Хотите добавить больше компонентов?

Если нужны дополнительные компоненты - просто скажите какие, и я их создам!

---

**Успехов в разработке! 🚀**


